<!DOCTYPE html>
<html>
  <head>
    <title>RailsFinal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  
    <header class="my-0">

      <!-- jumbotron -->
      <div class="jumbotron jumbotron-fluid bg-warning text-white my-0 barcad">
        <div class="container text-sm-center">
          <h1 class="display-1 ">(FITNEST)</h1>
          <p class="lead">Working out is better with friends, find your swolemates</p>
        </div>
      </div>

      <!-- navbar -->


      <nav class="navbar navbar-expand-lg navbar-dark bg-warning mt-0 p-0 border border-dark">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse text-center" id="navbarNavDropdown">  
          <ul class="navbar-nav mr-auto mt-2 pt-2 mt-lg-0">
            <h6 class="navbar-text barcad mx-2 text-light">(FITNEST)</h6>
            <%if user_signed_in?%>
              <li class="nav-item dropdown dmenu">
                <a class="nav-link dropdown-toggle text-white" href="#" id="navbardrop" data-toggle="dropdown">
                  Groups
                </a>
                <div class="dropdown-menu sm-menu">
                  <%= link_to 'Groups', groups_path, class: "dropdown-item" %>
                  <%= link_to 'New Group', new_group_path, class: "dropdown-item" %>
                </div>
              </li>
              <li class="nav-item">
                  <%= link_to 'My Profile', user_path(current_user.id), class: "nav-link text-light" %>
              </li>
              <li class="nav-item">
                <%= link_to 'Log Out', signout_path, class: "nav-link text-light" %> 
              </li>
            <% end %>  
          </ul>
        <div>  
        <form class="form-inline my-2 my-lg-0" action="/search">
          <input class="form-control mr-sm-2" name="q" type="text" placeholder="Search groups by activity" aria-label="Search Groups">
          <button class="btn btn-outline-secondary my-2 my-sm-0 mr-2" type="submit">Search</button>
        </form>
      </nav>

    </header>

    <div class="container">
      <%= yield %>
    </div>

    <footer class="footer bg-warning border-top border-dark pt-2">
    <p>&copy; 2021 fitnest.com || Hiphopopotamus Studios </p> 
    </footer>
    
  
  </body>
</html>
